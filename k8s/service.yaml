apiVersion: v1
kind: Service
metadata:
  name: backstage-service
  namespace: feina  # TODO: migrar a apis-asincronas
spec:
  type: LoadBalancer
  selector:
    app: backstage
  ports:
    - name: http
      protocol: TCP
      port: 80
      targetPort: 7007
