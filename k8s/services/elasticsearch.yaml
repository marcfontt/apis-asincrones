apiVersion: v1
kind: Service
metadata:
  name: elasticsearch
  namespace: apis-asincronas
spec:
  type: ClusterIP
  selector:
    app: elasticsearch
  ports:
  - port: 9200
    protocol: TCP
    targetPort: 9200
