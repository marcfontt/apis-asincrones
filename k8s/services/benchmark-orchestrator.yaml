apiVersion: v1
kind: Service
metadata:
  name: benchmark-orchestrator
  namespace: apis-asincrones
spec:
  type: ClusterIP
  selector:
    app: benchmark-orchestrator
  ports:
  - port: 3003
    protocol: TCP
    targetPort: 3003
