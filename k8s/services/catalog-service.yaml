apiVersion: v1
kind: Service
metadata:
  name: catalog-service
  namespace: apis-asincronas
spec:
  type: ClusterIP
  selector:
    app: catalog-service
  ports:
  - port: 3001
    protocol: TCP
    targetPort: 3001
