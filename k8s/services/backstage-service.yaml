apiVersion: v1
kind: Service
metadata:
  name: backstage-service
  namespace: apis-asincrones
spec:
  type: LoadBalancer
  selector:
    app: backstage
  ports:
  - name: http
    port: 80
    protocol: TCP
    targetPort: 7007
