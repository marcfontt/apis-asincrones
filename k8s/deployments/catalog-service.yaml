apiVersion: apps/v1
kind: Deployment
metadata:
  name: catalog-service
  namespace: apis-asincrones
spec:
  progressDeadlineSeconds: 600
  replicas: 1
  revisionHistoryLimit: 10
  selector:
    matchLabels:
      app: catalog-service
  strategy:
    type: Recreate
  template:
    metadata:
      labels:
        app: catalog-service
    spec:
      imagePullSecrets:
      - name: acr-secret
      containers:
      - name: catalog-service
        image: asyncpfgpayg65454.azurecr.io/catalog-service:latest
        imagePullPolicy: Always
        env:
        - name: ELASTICSEARCH_URL
          value: http://elasticsearch:9200
        - name: PORT
          value: "3001"
        ports:
        - containerPort: 3001
          protocol: TCP
        resources:
          limits:
            cpu: 250m
            memory: 192Mi
          requests:
            cpu: 25m
            memory: 96Mi
