apiVersion: apps/v1
kind: Deployment
metadata:
  name: metrics-api
  namespace: apis-asincrones
spec:
  progressDeadlineSeconds: 600
  replicas: 1
  revisionHistoryLimit: 10
  selector:
    matchLabels:
      app: metrics-api
  strategy:
    rollingUpdate:
      maxSurge: 25%
      maxUnavailable: 25%
    type: RollingUpdate
  template:
    metadata:
      labels:
        app: metrics-api
    spec:
      imagePullSecrets:
      - name: acr-secret
      containers:
      - name: metrics-api
        image: asyncbenchmarkregistry.azurecr.io/metrics-api:latest
        imagePullPolicy: Always
        env:
        - name: ELASTICSEARCH_URL
          value: http://elasticsearch:9200
        - name: PORT
          value: "3004"
        ports:
        - containerPort: 3004
          protocol: TCP
        resources:
          limits:
            cpu: 500m
            memory: 256Mi
          requests:
            cpu: 100m
            memory: 128Mi
